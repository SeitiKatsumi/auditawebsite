import type { MetadataRoute } from "next"; export default function sitemap(): MetadataRoute.Sitemap { const base=process.env.NEXT_PUBLIC_SITE_URL||"https://audita.com.br"; return ["/analise-de-vendedor","/analise-de-vendedor-2","/analise-cobrancas-indevidas","/politica-de-privacidade","/termos-de-uso"].map((path)=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path.includes("analise")?"weekly":"yearly",priority:path.includes("analise")?1:.3})); }
